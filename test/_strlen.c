#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - Entry
 * Description:  a function that returns the length of a string.
 * @s: a pointer to int
 * Return: length of a string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
